build() {
  ./scripts/install.sh -p "$PREFIX" -b "${HOME}/.bashrc.local"
}

clean() {
  ./scripts/uninstall.sh -p "$PREFIX" -b "${HOME}/.bashrc.local"
}

get() {
  git clone https://github.com/erhlee-bird/jmp .
}
